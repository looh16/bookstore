export default class BookClass {
  /* eslint-disable camelcase */
  constructor(item_id, title, author, category) {
    this.item_id = item_id;
    this.title = title;
    this.author = author;
    this.category = category;
  }
}
